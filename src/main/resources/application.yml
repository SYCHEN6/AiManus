spring:
  application:
    name: AI-Agent
  profiles:
    active: local
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

# Agent 配置
agent:
  token-limit: 30000

# RAG 配置
rag:
  ingest-on-startup: false   # 首次运行前改为 true，完成后改回 false 避免重复写入
  embed-model: text-embedding-v3
#  ai:
#    mcp:
#      client:
#        stdio:
#          servers-configuration: classpath:mcp-servers.json  # Linux/Mac/CI 使用，local profile 会覆盖此配置
#  mail:
#    host: smtp.qq.com
#    port: 465
#    username: 邮箱       # 替换为你的 QQ 邮箱
#    password: 授权码 # 替换为 QQ 邮箱授权码（非登录密码）
#    protocol: smtp
#    properties:
#      mail:
#        smtp:
#          ssl:
#            enable: true
#          auth: true
#  ai:
#    dashscope:
#      # 需要替换为你自己的 key
#      api-key: key
#      chat:
#        options:
#          model: MiniMax-M2.5
  # ollama??????
#  ai:
#    ollama:
#      base-url: http://localhost:11434
#      chat:
#        model: gemma3:1b
server:
  port: 9888
  servlet:
    context-path: /rest # ??????rest??
# springdoc-openapi ??????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.study.aiagent.controller # ????????
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
#logging:
#  level:
#    io.modelcontextprotocol: DEBUG
#    org.springframework.ai.mcp: DEBUG
#    org.springframework.ai.autoconfigure.mcp: DEBUG
#search:
#  api-key: key # 替换为你的搜索api key
